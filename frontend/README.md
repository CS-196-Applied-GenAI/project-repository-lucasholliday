# Chirper Frontend

Frontend app for the Chirper project using React + Vite + TypeScript.

## Commands

From `frontend/`:

```bash
npm install
npm run dev
npm test
npm run test:coverage
npm run build
```

## Environment

Create `.env` in `frontend/` with:

```env
VITE_API_BASE_URL=http://localhost:8000
```
